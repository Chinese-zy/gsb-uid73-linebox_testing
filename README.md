先装依赖，不要用容器。

npm install
node test/shot.test.js

页面直接打开 index.html。字宽、行高、盒子宽都写在页面上。
整页比对那条会红，窗口或字体一变就对不上，不要拿它当布局已经坏了。

不比对整页图、可重跑的布局断言：

npm test

只量写死的结构值：从哪里断行、每个字坐哪条 16px 基线、超出切在哪个像素（clipAt）。
覆盖父级零高、空串、一个词比盒子还长、量到一半字宽或盒宽被改掉。
挂了会直接打印是哪条。
